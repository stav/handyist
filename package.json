{
  "name": "handyist",
  "description": "A static site generator for The Handyist",
  "private": true,
  "version": "0.0.0",
  "packageManager": "bun@1.0.25",
  "author": {
    "name": "Steven Almeroth",
    "email": "steven.almeroth@gmail.com"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "tailw": "bunx tailwindcss -i src/tailwind.css --content=./src/_includes/* -o dist/css/tailwind.css",
    "watch": "bunx @11ty/eleventy --serve --incremental --watch",
    "serve": "bun run clean && bun run tailw && bun run watch",
    "build": "bun run clean && bun run tailw && bunx @11ty/eleventy",
    "build:prod": "bun run clean && bun run tailw && ELEVENTY_ENV=production bunx @11ty/eleventy"
  },
  "peerDependencies": {
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
